# 🌌 Simple Start Page

一个极简、高颜值、功能强大的浏览器起始页。支持拖拽布局、卡片缩放、远程配置同步、内网网址切换等功能。

> Designed & Crafted by Gemini 🤖

## ✨ 特性 (Features)

### 🎨 唯美设计
基于磨砂玻璃 (Glassmorphism) 风格，配合 Bing 每日壁纸。

![start](start.png)

### 📐 自由布局
- **拖拽排序**：在编辑模式下，随意拖动卡片或链接调整顺序。
- **卡片缩放**：支持拖动卡片右下角调整大小 (Dashboard 仪表盘风格)，自动适配网格布局。

### 🛠️ 编辑模式
- 输入 `>edit` 进入编辑模式。
- 可视化的添加、修改、删除卡片和链接。
- **隐藏功能**：点击眼睛图标隐藏不想显示的卡片或项目。
- **导出配置**：本地修改后可一键导出 YAML，实现闭环同步。

### ☁️ 配置云同步
- 数据通过 YAML 格式管理。
- 支持从 GitHub Gist 远程加载配置。

### 🏠 内网/公网无缝切换
- 专为 HomeLab 玩家设计。
- 配置双链接后，点击项目图标即可一键在内网 IP 和公网域名间切换 (图标变绿提示)。

### 🚀 极速体验
- 优先读取本地缓存，毫秒级启动。
- **命令行交互**：
    - `>sync` (同步)
    - `>config` (设置源)
    - `>reset` (重置)
    - `>help` (帮助)

## 📂 项目结构 (Structure)

```text
.
├── index.html      # 页面入口
├── css/
│   └── style.css   # 样式文件 (Tailwind + Custom CSS)
├── js/
│   └── app.js      # 核心逻辑 (Sortable, Resize, Sync)
└── README.md       # 说明文档
└── nav.yaml        # 配置文件
```

## 🚀 快速部署 (Deployment)

### Vercel (推荐)
1. Fork 本仓库。
2. 在 Vercel 中导入该仓库。
3. 点击 Deploy，无需任何额外配置。

### 本地运行
直接双击打开 `index.html` 即可使用。

## ⚙️ 配置指南 (Configuration)

### 1. 创建配置文件
在 GitHub Gist 上创建一个 `nav.yml` 文件，参考格式如下：

```yaml
search:
  - name: "Google"
    url: "https://www.google.com/search?q="
    icon: "ph-google-logo"

categories:
  - category: "My Lab"
    color: "from-blue-600/20 to-indigo-600/20"
    colSpan: 2  # 可选：卡片宽度 (1-4)
    rowSpan: 1  # 可选：卡片高度 (1-2)
    hidden: false # 可选：是否隐藏
    items:
      - name: "Router"
        url: "https://router.com"
        url_private: "http://192.168.1.1" # 可选：内网地址
        icon: "ph-router"
        hidden: false # 可选：是否隐藏
```

### 2. 绑定配置
打开网页，在搜索框输入命令：
```bash
>config https://gist.githubusercontent.com/你的用户名/.../raw/nav.yml
```

### 3. 同步更新
当你更新了 Gist 文件后，在网页输入：
```bash
>sync
```

## ⌨️ 快捷命令 (Commands)

| 命令 | 说明 |
| --- | --- |
| `>edit` | 进入 UI 编辑模式 (支持拖拽排序、卡片缩放、增删改) |
| `>sync` | 强制从远程 URL 同步配置 |
| `>config [url]` | 设置远程配置文件地址 |
| `>reset` | 清除本地缓存和配置，恢复出厂 |
| `>help` | 显示帮助菜单 (或点击时钟) |

---
Created with ❤️ by Gemini